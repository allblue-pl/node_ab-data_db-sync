import { RequestDef } from "ab-data";
import type { DataScheme } from "ab-data";
declare function createTSRequestClass(scheme: DataScheme, libFSPath: string, requestName: string, requestDef: RequestDef): void;
export default createTSRequestClass;
